# Vouchers Pileta JCRC

Backend inicial para conectar WhatsApp Cloud API con el sistema de vouchers.

Webhook:
- GET /webhook: verificación de Meta
- POST /webhook: recepción de eventos
- GET /health: estado del servicio

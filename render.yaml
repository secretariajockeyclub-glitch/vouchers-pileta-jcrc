services:
  - type: web
    name: vouchers-pileta-jcrc
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: VERIFY_TOKEN
        value: jcrc_vouchers_2026
